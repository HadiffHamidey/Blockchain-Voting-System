#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@remix-project/remix-tests/bin/remix-tests" "$@"
  ret=$?
else 
  node  "$basedir/../@remix-project/remix-tests/bin/remix-tests" "$@"
  ret=$?
fi
exit $ret
